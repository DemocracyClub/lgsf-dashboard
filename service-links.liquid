---
layout: base.liquid
title: Service Links
permalink: /service-links/
---
<h1>Service Links</h1>

<p>This page shows council service links including their CMS types and base URLs.</p>

<p>
  <a href="{{ '/data/services.csv' | url }}" class="ds-cta" download>Download CSV</a>
</p>

<table class="ds-table" id="services">
  <thead>
    <tr>
      <th>Official Identifier</th>
      <th>Common Name</th>
      <th>Service Name</th>
      <th>CMS Type</th>
      <th>Base URL</th>
    </tr>
  </thead>
  <tbody>
    {% for service in services %}
      <tr>
        <td>{{ service.official_identifier }}</td>
        <td>{{ service.common_name }}</td>
        <td>{{ service.service_name }}</td>
        <td>{{ service.cms_type }}</td>
        <td><a href="{{ service.base_url }}" target="_blank" rel="noopener noreferrer">{{ service.base_url }}</a></td>
      </tr>
    {% endfor %}
  </tbody>
</table>

{% if services.size == 0 %}
  <p class="ds-warning">No service data available.</p>
{% endif %}
